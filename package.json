{
	"private": true,
	"name": "@slapdoodle/icons",
	"scripts": {
		"icons:build": "lerna run --scope=icons build --stream",
		"icons:release": "lerna run --scope=icons release --stream",
		"docs:start": "lerna run --scope=docs start --stream",
		"docs:build": "lerna run --scope=docs build --stream"
	},
	"workspaces": [
		"packages/*"
	],
	"devDependencies": {
		"lerna": "^2.8.0"
	}
}
